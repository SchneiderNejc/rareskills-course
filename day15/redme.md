Cost per signer is 5000 lamports.

Operations such as pushing elements to array costs more compute units, but does not affect our transaction fees. This is expected and shows that truly, compute units do not affect the transaction fees paid by users.

The larger the value types used, the larger the compute unit consumed. It’s best to use smaller types where applicable. 